# Writing GitHub Actions for Agents

- Use `paths` filters to trigger agents based on upstream handoffs.
- Fail fast: stop the workflow if any validation step fails.
- Log decision reasoning for auditability.
