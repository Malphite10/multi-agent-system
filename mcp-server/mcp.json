{
  "servers": {
    "default": {
      "env": {
        "API_KEY": "REDACTED_FROM_ENV",
        "DATABASE_URL": "REDACTED_FROM_ENV"
      },
      "allowlist": [
        "api.example.com"
      ]
    }
  }
}
